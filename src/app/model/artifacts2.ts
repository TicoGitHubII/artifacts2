export class Artifacts2 {
  
    id: number;
    title: string;
    date_start: number;

    constructor(title: string, date_start: number, id: number) {
        this.id = id;
        this.title = title;
        this.date_start = date_start;

      }  
  }
  